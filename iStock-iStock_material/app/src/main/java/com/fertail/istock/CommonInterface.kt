package com.fertail.istock

import android.content.DialogInterface

interface CommonInterface {
    fun btnPositiveSelected(dialog: DialogInterface)
    fun btnNegativeSelected(dialog: DialogInterface)
}
